image:
  tag: "1.2.7"
  pullPolicy: Always

imagePullSecrets:
  - name: docker-aiteza

ingress:
  enabled: true
  ingresscClassName: traefik
  hosts:
    - host: frontend.local
      path: /
  tls: []