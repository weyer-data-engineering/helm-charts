image:
  tag: "1.3.0"
  pullPolicy: Always

imagePullSecrets:
  - name: docker-aiteza

aiteza:
  backendUrl: http://backend.local
  keycloak:
    url: http://auth.local
    realm: local
    clientId: test

ingress:
  enabled: true
  ingresscClassName: traefik
  host: frontend.local
  tls: []

nodeSelector:
  run: application