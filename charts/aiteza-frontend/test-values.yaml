image:
  tag: "1.2.7"
  pullPolicy: Always

imagePullSecrets:
  - name: docker-aiteza

aiteza:
  backendUrl: http://backend.local
  keycloak:
    url: http://auth.local
    realm: local
    clientId: test

ingress:
  enabled: true
  ingresscClassName: traefik
  host: frontend.local
  tls: []